<div class="event-title-container">
    <h1 class="event-title">All Events</h1>
</div>
<!-- Event grid -->
<div class="grid">
    <div class="container event-grid-container">
        <div class="row">
            {{!-- event cards --}}
            {{#each events as |event|}}
            {{> card}}
            {{/each}}
        </div>
    </div>
</div>


{{#each events as |event|}}
{{> expandedCard}}
{{/each}}